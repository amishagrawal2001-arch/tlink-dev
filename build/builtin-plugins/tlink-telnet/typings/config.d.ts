import { ConfigProvider } from 'tlink-core';
/** @hidden */
export declare class TelnetConfigProvider extends ConfigProvider {
    defaults: {
        hotkeys: {
            'restart-telnet-session': never[];
        };
    };
    platformDefaults: {};
}
